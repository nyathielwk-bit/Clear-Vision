A labelled checkbox with an optional description line — fills sienna when checked.

```jsx
<Checkbox label="Subscribe to the Journal" defaultChecked />
<Checkbox label="Gift wrapping" description="Add a hand-tied ribbon (+$6)." />
```

Accepts native checkbox props (`checked`, `defaultChecked`, `onChange`, `disabled`). Use `description` for supporting copy.
