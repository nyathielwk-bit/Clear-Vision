An on/off toggle switch — sienna when on. Use for settings and preferences.

```jsx
<Switch label="Email me about new issues" defaultChecked />
<Switch label="Pause membership" />
```

Accepts native checkbox props (`checked`, `defaultChecked`, `onChange`, `disabled`). Renders with `role="switch"`.
